# FlowForge Architecture

```text
app/
├── main.py
├── core/
├── components/
├── views/
├── modules/
├── utils/
└── assets/
```

`main.py` only starts the app. `core` controls application flow. `components` contains reusable UI. `views` assemble screens. `modules` contain business logic.
